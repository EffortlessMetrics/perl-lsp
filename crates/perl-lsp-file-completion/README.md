# perl-lsp-file-completion

Standalone SRP microcrate for secure string-literal file path completion in the Perl LSP ecosystem.

This crate owns one responsibility: convert a file path prefix into bounded, security-filtered completion items.
