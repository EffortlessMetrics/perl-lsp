# perl-lsp-feature-ids

A tiny microcrate that centralizes canonical feature identifiers used by LSP and DAP
feature-flagging and BDD-grid interoperability layers.

It intentionally stays minimal and dependency-free so it can be reused across
crates and tooling without introducing extra coupling.
