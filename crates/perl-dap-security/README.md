# perl-dap-security

Security-focused validation utilities used by `perl-dap`.

This microcrate isolates path, expression, and timeout validation so DAP
security controls can evolve independently from protocol orchestration.
