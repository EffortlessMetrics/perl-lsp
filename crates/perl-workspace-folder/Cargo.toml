[package]
name = "perl-workspace-folder"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation = "https://docs.rs/perl-workspace-folder"
description = "Parse Perl workspace folder declarations into filesystem paths"
readme = "README.md"
keywords = ["perl", "workspace", "path", "uri", "lsp"]
categories = ["development-tools", "filesystem"]
include = ["src/**", "tests/**", "Cargo.toml", "README.md", "LICENSE*"]

[dependencies]
perl-uri = { workspace = true }

[dev-dependencies]
proptest = "1.9.0"
tempfile = "3.24.0"
url = "2.5.8"

[lints]
workspace = true
