# perl-dap-protocol

Shared Debug Adapter Protocol (DAP) type definitions for the perl-lsp workspace.

This crate contains serializable request/response/event structures used by
`perl-dap` and related tooling.
