[package]
name = "perl-module-rename"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Plan deterministic Perl module import rewrites for rename workflows"
readme = "README.md"
documentation = "https://docs.rs/perl-module-rename"
keywords = ["perl", "module", "rename", "lsp", "workspace"]
categories = ["development-tools", "text-editors"]
include = ["src/**", "tests/**", "Cargo.toml", "README.md", "LICENSE*"]

[dependencies]
perl-module-token = { workspace = true }
perl-module-import-match = { workspace = true }

[dev-dependencies]
proptest = "1.9.0"
perl-module-path = { workspace = true }

[lints]
workspace = true
