[package]
name = "perl-uri"
version = "0.9.0"
edition.workspace = true
rust-version.workspace = true
authors = ["Tree-sitter Perl Contributors"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/EffortlessMetrics/tree-sitter-perl-rs"
homepage = "https://github.com/EffortlessMetrics/tree-sitter-perl-rs"
documentation = "https://docs.rs/perl-uri"
description = "URI ↔ filesystem path conversion and normalization utilities for Perl LSP"
readme = "README.md"
keywords = ["perl", "uri", "path", "lsp"]
categories = ["development-tools", "encoding"]
include = ["src/**", "Cargo.toml", "README.md", "LICENSE*"]

[dependencies]
url = "2.5.8"

[dev-dependencies]
tempfile = "3.24.0"
perl-tdd-support = { workspace = true }

[features]
default = []

[lints]
workspace = true

