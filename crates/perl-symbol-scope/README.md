# perl-symbol-scope

Shared scope primitives for Perl symbol analysis.

This crate provides:

- `ScopeId`: unique scope identifiers
- `ScopeKind`: lexical scope classification

It is intended for reuse by symbol-table and semantic-analysis crates.
