[package]
name = "perl-module-token-parser"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Single-responsibility Perl module token parsing for import/reference workflows"
readme = "README.md"
documentation = "https://docs.rs/perl-module-token-parser"
keywords = ["perl", "module", "token", "parser", "import"]
categories = ["development-tools", "text-editors"]
include = ["src/**", "tests/**", "Cargo.toml", "README.md", "LICENSE*"]

[dependencies]
perl-module-token-core = { workspace = true }

[dev-dependencies]
proptest = "1.10.0"
perl-module-reference = { workspace = true }

[lints]
workspace = true
