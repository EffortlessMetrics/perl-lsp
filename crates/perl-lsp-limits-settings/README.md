# perl-lsp-limits-settings

SRP microcrate that applies JSON editor settings overlays onto `perl-lsp-limits` structs.

This crate intentionally contains only settings decoding/overlay behavior so the core
limits crate can focus on defaults, profiles, and runtime accessors.
