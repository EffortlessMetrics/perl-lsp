//! Re-export of DAP protocol types.
//!
//! Protocol schema types now live in the dedicated `perl-dap-protocol` crate
//! to keep `perl-dap` focused on adapter orchestration/runtime behavior.

pub use perl_dap_protocol::*;
