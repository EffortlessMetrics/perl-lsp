[package]
name = "perl-module-token"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Boundary-safe Perl module token replacement and variant helpers"
readme = "README.md"
documentation = "https://docs.rs/perl-module-token"
keywords = ["perl", "module", "rename", "token", "lsp"]
categories = ["development-tools", "text-editors"]
include = ["src/**", "tests/**", "Cargo.toml", "README.md", "LICENSE*"]

[dependencies]
perl-module-boundary = { workspace = true }
perl-module-name = { workspace = true }

[dev-dependencies]
proptest = "1.10.0"
perl-module-path = { workspace = true }

[lints]
workspace = true
