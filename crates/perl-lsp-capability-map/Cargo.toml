[package]
name = "perl-lsp-capability-map"
version.workspace = true
edition = { workspace = true }
rust-version.workspace = true
authors = { workspace = true }
license = "MIT OR Apache-2.0"
repository = { workspace = true }
homepage = { workspace = true }
description = "SRP microcrate for translating between LSP ServerCapabilities and feature IDs."
documentation = "https://docs.rs/perl-lsp-capability-map"
readme = "README.md"
keywords = ["perl", "lsp", "capabilities", "features", "mapping"]
categories = ["development-tools"]
include = ["src/**", "Cargo.toml", "README.md"]

[dependencies]
lsp-types.workspace = true
perl-lsp-feature-ids = { workspace = true }

[dev-dependencies]
serde_json.workspace = true

[package.metadata.docs.rs]
rustdoc-args = ["--cfg", "docsrs"]

[lints]
workspace = true
