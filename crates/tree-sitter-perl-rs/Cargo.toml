[package]
name = "tree-sitter-perl"
version = "0.0.1"
edition = "2024"
description = "Tree-sitter Perl grammar with Rust-native scanner"
license = "MIT"
repository = "https://github.com/EffortlessSteven/tree-sitter-perl-rs"
keywords = ["tree-sitter", "perl", "parser", "grammar"]
categories = ["parsing", "text-processing"]

[dependencies]
tree-sitter = "0.25"
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
proptest = "1.3"
walkdir = "2.4"
thiserror = "1.0"
unicode-ident = "1.0"
unicode-normalization = "0.1"

[dev-dependencies]
criterion = "0.5"
test-case = "3.3"

[features]
default = ["rust-scanner"]
rust-scanner = []
c-scanner = []
test-utils = []

[lib]
name = "tree_sitter_perl"
path = "src/lib.rs"

[[bin]]
name = "bench_parser"
path = "src/bin/bench_parser.rs"
required-features = ["test-utils"]

[[bench]]
name = "scanner_benchmarks"
path = "../../benches/scanner_benchmarks.rs"
harness = false

[[bench]]
name = "parser_benchmarks"
path = "../../benches/parser_benchmarks.rs"
harness = false

[build-dependencies]
cc = "1.0"
bindgen = "0.69" 