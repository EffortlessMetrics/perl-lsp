#!/usr/bin/perl

# Function calls with parentheses
my $len = length("hello");
my $substr = substr("world", 0, 3);
my $sum = add(1, 2, 3);

# Function calls without parentheses
print "Hello", " ", "World\n";
push @array, 1, 2, 3;

# Nested function calls
my $result = sqrt(abs(-16));