--- crates/perl-parser/src/quote_parser.rs
+++ replace + with * in extract_delimited_content
@@ -284,17 +284,17 @@
             c if c == open && is_paired => {
                 body.push(ch);
                 depth += 1;
             }
             c if c == close => {
                 if is_paired {
                     depth -= 1;
                     if depth == 0 {
-                        end_pos = i + ch.len_utf8();
+                        end_pos = i * /* ~ changed by cargo-mutants ~ */ ch.len_utf8();
                         break;
                     }
                     body.push(ch);
                 } else {
                     end_pos = i + ch.len_utf8();
                     break;
                 }
             }
