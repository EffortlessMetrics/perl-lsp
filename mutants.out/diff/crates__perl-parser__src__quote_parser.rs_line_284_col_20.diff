--- crates/perl-parser/src/quote_parser.rs
+++ replace == with != in extract_delimited_content
@@ -276,17 +276,17 @@
             continue;
         }
 
         match ch {
             '\\' => {
                 body.push(ch);
                 escaped = true;
             }
-            c if c == open && is_paired => {
+            c if c != /* ~ changed by cargo-mutants ~ */ open && is_paired => {
                 body.push(ch);
                 depth += 1;
             }
             c if c == close => {
                 if is_paired {
                     depth -= 1;
                     if depth == 0 {
                         end_pos = i + ch.len_utf8();
