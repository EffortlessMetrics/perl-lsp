--- crates/perl-parser/src/quote_parser.rs
+++ replace match guard c == close with true in extract_delimited_content
@@ -280,17 +280,17 @@
             '\\' => {
                 body.push(ch);
                 escaped = true;
             }
             c if c == open && is_paired => {
                 body.push(ch);
                 depth += 1;
             }
-            c if c == close => {
+            c if true /* ~ changed by cargo-mutants ~ */ => {
                 if is_paired {
                     depth -= 1;
                     if depth == 0 {
                         end_pos = i + ch.len_utf8();
                         break;
                     }
                     body.push(ch);
                 } else {
