--- crates/perl-parser/src/quote_parser.rs
+++ delete match arm '\\' in extract_transliteration_parts
@@ -204,20 +204,17 @@
         for (i, ch) in chars {
             if escaped {
                 body.push(ch);
                 escaped = false;
                 continue;
             }
 
             match ch {
-                '\\' => {
-                    body.push(ch);
-                    escaped = true;
-                }
+                 /* ~ changed by cargo-mutants ~ */
                 c if c == closing => {
                     end_pos = i + ch.len_utf8();
                     break;
                 }
                 _ => body.push(ch),
             }
         }
 
