# Provider Confidence Receipt Matrix

> Human-owned. This matrix summarizes provider confidence receipts across the
> status stack. It does not generate metrics, broaden live behavior, or replace
> provider-specific proof.

This matrix answers:

> Which provider surfaces have source, provenance, confidence, freshness,
> fallback, runtime comparison, and next-proof boundaries?

Update the underlying proof first, then refresh this matrix. Generated receipt
details remain in [semantic shadow compare](semantic_shadow_compare.md),
[semantic scorecard](semantic_scorecard.md), and parser/status artifacts.
Provider cutover state remains in [provider cutover](provider_cutover.md).
Real-workspace timing proof lives in the
[2026-05-13 Mojolicious baseline](../../forensics/2026-05-13-real-workspace-baseline-mojolicious.md).
User-facing support claims are mapped in [SUPPORT_TIERS.md](SUPPORT_TIERS.md).
The Real Perl Editor Trust v1 routing dashboard is
[real_perl_editor_trust_v1.md](real_perl_editor_trust_v1.md).

## Claim Boundary

This page is a control-plane summary. A row may say that a provider has
confidence or fallback receipts. It must not be read as a broad live cutover
claim unless the live state and next-proof columns say so explicitly.

## Matrix

| Provider surface | Live state | Fact source / provenance | Confidence and freshness boundary | Fallback / blocker behavior | Runtime or live comparison receipt | Real-workspace link | Next proof |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Completion | `partial live / shadowed` | `CompilerFact` from `ImportExportInference`; generated candidates from `FrameworkAdapter`; dynamic blockers from `DynamicBoundary` | High-confidence, fresh imported/exported visible-symbol facts may contribute live candidates; generated and dynamic candidates remain shadowed or blocked | Legacy completion remains fallback; generated and dynamic-boundary candidates are not promoted into broad live completion | [semantic_shadow_compare.md](semantic_shadow_compare.md) `CompletionVisibility`; [provider_cutover.md](provider_cutover.md) completion row; [#8374](https://github.com/EffortlessMetrics/perl-lsp/issues/8374); [#8891](https://github.com/EffortlessMetrics/perl-lsp/pull/8891) | Mojolicious scenario 28 records visible-symbol ranking quality: candidate counts, top-N churn, useful/noisy additions, generated labels, and dynamic/fallback labels | Additional project-shape and broader generated/dynamic/method ranking receipts before broader completion cutover |
| Goto definition | `partial live exact/imported` | `ExactAst`, explicit import/export facts from `ImportExportInference`, generated candidates from `FrameworkAdapter`, fallback from `NameHeuristic` | A single high-confidence, fresh, source-backed exact/import/export candidate may answer live; stale, low-confidence, ambiguous, generated/no-source, and dynamic cases do not claim exact source locations | Legacy definition remains fallback for uncertain candidates; dynamic and stale facts block compiler-backed exact navigation | [provider_cutover.md](provider_cutover.md#navigation-live-quality-dashboard); [semantic_shadow_compare.md](semantic_shadow_compare.md) `FindDefinition`; [#8803](https://github.com/EffortlessMetrics/perl-lsp/issues/8803), [#8462](https://github.com/EffortlessMetrics/perl-lsp/issues/8462) | Shadow compare records navigation real-workspace quality candidates; Mojolicious baseline covers goto latency only | Broader generated/dynamic navigation quality with no false-exact source-location claims |
| References | `partial live exact/imported` | `ExactAst`, `ImportExportInference`, and `LiteralRequireImport` occurrences; generated candidates from `FrameworkAdapter`; fallback from `NameHeuristic` | High-confidence, fresh, source-backed occurrences may answer live when declaration inclusion is off; stale, low-confidence, ambiguous, generated/no-source, declaration-including, coderef, typeglob, and dynamic cases remain non-live | Legacy references remain fallback for uncertain candidates; dynamic and stale facts block compiler-backed exact references | [provider_cutover.md](provider_cutover.md#navigation-live-quality-dashboard); [semantic_shadow_compare.md](semantic_shadow_compare.md) `FindReferences`; [#8828](https://github.com/EffortlessMetrics/perl-lsp/issues/8828), [#8836](https://github.com/EffortlessMetrics/perl-lsp/issues/8836), [#8462](https://github.com/EffortlessMetrics/perl-lsp/issues/8462) | Shadow compare records references real-workspace quality candidates; broader precision/recall remains pending | Generated, coderef, typeglob, declaration-including, and dynamic-boundary precision/recall receipts |
| Hover | `partial live / provenance-backed` | Imported compiler facts from `ImportExportInference`, generated facts from `FrameworkAdapter`, dynamic blockers from `DynamicBoundary`, fallback from `SearchFallback` | Fresh high-confidence imported facts and selected generated/dynamic provenance paths are labeled; fallback remains explicit when facts are absent or legacy-equivalent | Legacy hover remains fallback; dynamic boundaries are labeled/blocked instead of becoming exact static claims | [semantic_shadow_compare.md](semantic_shadow_compare.md) `Hover`; [provider_cutover.md](provider_cutover.md) hover row; [#8369](https://github.com/EffortlessMetrics/perl-lsp/issues/8369) | Mojolicious baseline covers first-hover latency; hover correctness and generated/dynamic quality are not promoted by that receipt alone | Real-workspace hover quality receipts before broader generated or dynamic expansion |
| Diagnostics | `partial live` | Compiler facts from `ImportExportInference`, `FrameworkSynthesis`, and `SemanticAnalyzer`; blockers from `DynamicBoundary` | High-confidence, fresh imported/generated facts may suppress selected false positives; ambiguous or low-confidence facts do not suppress true unknowns | Legacy diagnostics remain fallback; missing or ambiguous semantic evidence keeps conservative diagnostics | [semantic_scorecard.md](semantic_scorecard.md); [semantic_shadow_compare.md](semantic_shadow_compare.md) `DiagnosticsCheck`; dynamic diagnostics tests linked from [ux_capability_dashboard.md](ux_capability_dashboard.md) | Mojolicious baseline explicitly defers diagnostic correctness | Broader false-positive and false-negative fixture receipts plus real-workspace diagnostic correctness proof |
| Rename | `boundary-shadowed` | Exact static plans from `ExactAst`; dynamic blockers from `DynamicBoundary`; stale compiler facts from `SemanticAnalyzer`; low-confidence ambiguity from `NameHeuristic` | Exact static plans are receipt-backed; stale, low-confidence, generated, and dynamic facts cannot authorize edits | Runtime blocker UX receipts require block/refuse behavior for stale, low-confidence, generated, and dynamic cases; live rename remains the existing provider path | [semantic_shadow_compare.md](semantic_shadow_compare.md) `RenamePlan`; [semantic_scorecard.md](semantic_scorecard.md); [provider_cutover.md](provider_cutover.md); [#8464](https://github.com/EffortlessMetrics/perl-lsp/issues/8464) | Real-workspace unsafe-edit proof remains pending | Real-workspace unsafe-edit receipts and a narrow lexical/package rename live-cutover proof with rollback |
| Safe delete | `boundary-shadowed` | Exact static plans from `ExactAst`; generated blockers from `FrameworkAdapter`; dynamic blockers from `DynamicBoundary`; stale compiler facts from `SemanticAnalyzer` | Exact static allow decisions are receipt-backed; generated, stale, low-confidence, and dynamic facts cannot authorize deletion | Runtime blocker UX receipts compare compiler plans with the live no-edit baseline; no live symbol-level safe-delete provider is claimed | [semantic_shadow_compare.md](semantic_shadow_compare.md) `SafeDeletePlan`; [semantic_scorecard.md](semantic_scorecard.md); [provider_cutover.md](provider_cutover.md); [#8464](https://github.com/EffortlessMetrics/perl-lsp/issues/8464) | Real-workspace unsafe-delete proof remains pending | Real-workspace unsafe-delete receipts and explicit live blocker UX before any symbol-level cutover |
| Workspace symbols | `shadowed` | Fresh compiler facts from `ImportExportInference`; generated candidates from `FrameworkAdapter`; dynamic blockers from `DynamicBoundary`; stale facts from `SemanticAnalyzer` | Fresh compiler candidates are shadowed; stale and dynamic candidates block compiler-backed live claims | Existing workspace index remains live; runtime quality receipts capture live counts/results without changing behavior | [semantic_shadow_compare.md](semantic_shadow_compare.md) `WorkspaceSymbols`; [provider_cutover.md](provider_cutover.md); [#8378](https://github.com/EffortlessMetrics/perl-lsp/issues/8378) | Shadow compare records workspace-symbol real-workspace quality; Mojolicious baseline covers workspace-symbol latency | Live-provider quality receipts and noise/rank deltas before workspace-symbol cutover |
| Document symbols | `shadowed` | Parser syntax from `ExactAst`; generated candidates from `FrameworkAdapter`; dynamic blockers from `DynamicBoundary`; stale facts from `SemanticAnalyzer` | Parser and compiler candidates remain shadow-only for cutover purposes; stale and dynamic candidates block compiler-backed live claims | Existing document-symbol provider remains live; runtime quality receipts capture live counts/results without changing behavior | [semantic_shadow_compare.md](semantic_shadow_compare.md) `DocumentSymbols`; [provider_cutover.md](provider_cutover.md) document-symbol row | No dedicated real-workspace document-symbol quality receipt yet | Real-workspace document-symbol quality receipts before live cutover |
| Semantic tokens | `shadowed` | Parser/HIR classification from `ExactAst`; compiler-backed classification from `SemanticAnalyzer`; blockers from `DynamicBoundary` and stale compiler facts | Parser and compiler classifications remain shadow-only for cutover purposes; stale and dynamic candidates block compiler-backed live claims | Existing parser/token provider remains live; runtime quality receipts capture token counts and shadow state without changing behavior | [semantic_shadow_compare.md](semantic_shadow_compare.md) `SemanticTokens`; [provider_cutover.md](provider_cutover.md) semantic-token row | No dedicated real-workspace semantic-token quality receipt yet | Token/span invariant receipts and real-workspace token quality before live cutover |
| DAP module paths / Perl subprocess seams | `provenance-backed` | `PerlOracleEnv` subprocess contract, module-resolution smoke tests, explicit language/version/startup probe seams | Environment provenance is explicit; ambient `PERL5LIB` and `PERL5OPT` are denied unless the seam opts in through configuration | DAP and language-probe subprocesses use typed environment construction instead of inheriting ambient process state; unsupported seams stay tracked in the PerlOracle rail | [PerlOracle rail](../../development/PERL_ORACLE_RAIL.md); [subprocess seams](../../architecture/perl-subprocess-seams.md); [0.14.0 readiness](../../releases/0.14.0-readiness.md) | Real-workspace editor baseline does not prove DAP subprocess behavior | Keep remaining Perl subprocess seams on the PerlOracle rail and add DAP workspace smoke when module-path behavior changes |

## Operating Rules

- Use this matrix to choose the next provider receipt lane.
- Update provider-specific proof before promoting a row.
- Do not move a row to broader live behavior without fallback and rollback
  proof.
- Do not use real-workspace latency alone as provider correctness proof.
- Keep generated status rows in their generators; this page links to them.
