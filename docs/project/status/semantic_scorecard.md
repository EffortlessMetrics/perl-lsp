# Semantic Scorecard (Harness Baseline)

This file is generated by `cargo xtask semantic-scorecard`.

## Summary

- Fixture families: 12
- Metrics defined: 11

## How to extend

- Add a new `[[fixture_family]]` row to `xtask/fixtures/semantic_scorecard/fixtures.toml`.
- Add or update `[[metric]]` rows with `status = "implemented"` when a metric becomes live.
- Re-run `cargo xtask semantic-scorecard` and commit both JSON + markdown outputs.

## Fixture families

- `autoload_dynamic_boundary` — AUTOLOAD dynamic boundary (baseline_pending)
- `dynamic_require_boundary` — dynamic require boundary (baseline_pending)
- `empty_import_suppression` — empty import suppression (baseline_pending)
- `eval_string_dynamic_boundary` — eval STRING dynamic boundary (baseline_pending)
- `export_tag_expansion` — export tag expansion (baseline_pending)
- `generated_accessor` — generated accessor (baseline_pending)
- `imported_function_visibility` — imported function visibility (baseline_pending)
- `inherited_method` — inherited method (baseline_pending)
- `qualified_vs_bare_references` — qualified vs bare references (baseline_pending)
- `role_method` — role method (baseline_pending)
- `same_bare_sub_two_packages` — same bare sub in two packages (baseline_pending)
- `typeglob_alias` — typeglob alias (baseline_pending)

## Metrics

- `completion_top1` [ratio] — not_implemented (baseline pending)
- `completion_top5` [ratio] — not_implemented (baseline pending)
- `definition_hit_at_1` [ratio] — not_implemented (baseline pending)
- `definition_hit_at_5` [ratio] — not_implemented (baseline pending)
- `query_latency_p50` [milliseconds] — not_implemented (baseline pending)
- `query_latency_p95` [milliseconds] — not_implemented (baseline pending)
- `reference_precision` [ratio] — not_implemented (baseline pending)
- `reference_recall` [ratio] — not_implemented (baseline pending)
- `rename_unsafe_edit_count` [count] — not_implemented (baseline pending)
- `safe_delete_external_ref_detection` [ratio] — not_implemented (baseline pending)
- `undefined_symbol_false_positive_rate` [ratio] — not_implemented (baseline pending)
