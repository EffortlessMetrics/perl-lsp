on:
  pull_request:
  push:
    branches: [master]

concurrency:
  group: required-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
