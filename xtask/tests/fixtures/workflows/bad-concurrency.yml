on:
  pull_request:
  merge_group:
  push:
    branches: [master]

concurrency:
  group: required-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
