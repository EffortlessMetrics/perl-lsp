WHITESPACE = _{ " " | "\t" | "\n" | "\r" }

format_declaration = {
    "format" ~ identifier? ~ "=" ~ "\n" ~
    format_line* ~
    "." ~ "\n"?
}

identifier = @{ ASCII_ALPHA ~ ASCII_ALPHANUMERIC* }
format_line = { (!("." ~ "\n") ~ ANY)+ ~ "\n" }

test = { SOI ~ format_declaration ~ EOI }